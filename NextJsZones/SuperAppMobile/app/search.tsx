import { SearchScreen } from '@search/mobile';

// This imports the Search module from Modules/Search/app/mobile
// In a true microfrontend setup with re.pack, this would be loaded dynamically from a remote server
export default SearchScreen;
